$ = {
    each: function(array, callback) {
        array.forEach(callback);
    }
}

for (i = 0; i < results.length; i++) {
    alert("you answered question 1" + results[i]);
}


alert(JSON.stringify("you answered question 1" + results[i]));